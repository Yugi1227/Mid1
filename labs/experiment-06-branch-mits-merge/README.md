# Experiment 06: Create MITS Branch and Merge

## Aim
Create a branch named `MITS`, make a change, commit it, and merge it into `main`.

## Run

```powershell
.\run.ps1
```

## What It Does
- Creates a demo repository.
- Creates and switches to the `MITS` branch.
- Commits a file change.
- Merges `MITS` back into `main`.

## Expected Result
The `MITS` branch is merged successfully and the log shows both commits.
