# Experiment 10: Inspect Google Element with Selenium (Edge)

## Run

```powershell
.\run.ps1
```

If `msedgedriver.exe` is not in the project root, set `EDGE_DRIVER_PATH` first.

## Expected Result
- Edge opens Google homepage.
- Script finds the search box and prints the WebElement.
