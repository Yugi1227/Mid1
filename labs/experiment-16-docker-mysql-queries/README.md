# Experiment 16: MySQL in Docker with Simple Query

## Aim
Run MySQL in Docker and execute a simple SQL file.

## Run

```powershell
.\run.ps1
```

## Stop

```powershell
docker rm -f exp16-mysql
```

## Expected Result
The `students` table is created and one row is selected from MySQL.
